# 项目因子与策略说明

本文档说明当前项目中已经实现的量化因子、回测策略和模拟实盘策略。内容以现有代码为准，主要对应以下模块：

- 回测引擎：`quant_backend/backtest/engine.py`
- 回测接口：`quant_backend/backtest/views.py`
- 实盘策略执行器：`quant_backend/trade/executor.py`
- 实盘调度与撮合：`quant_backend/trade/engine.py`、`quant_backend/trade/tasks.py`
- 前端参数配置：`quant_frontend/src/views/BacktestView.vue`、`quant_frontend/src/components/MyStrategies.vue`

## 1. 当前策略总体设计

项目当前采用的是一个**每日多因子轮动策略**。策略根据股票池内每只股票的历史收盘价计算多个内置因子，再对同一交易日的所有股票做横截面标准化，得到综合分数，最后每天按综合分从高到低选出 Top N，并结合买入阈值决定是否持仓或空仓。

当前主要启用的内置因子是：

1. **20 日动量因子**：衡量股票中期趋势强度。
2. **5 日反转因子**：捕捉短期回调后的均值回归机会。
3. **20 日低波动因子**：偏好收益波动更小的股票。
4. **20 日均线趋势因子**：衡量价格相对 20 日均线的强弱。

策略既用于 Spark 历史回测，也用于模拟实盘自动调仓。两者使用相同的核心思想，但执行环境不同：

- 回测使用 PySpark 对历史日线数据并行计算，生成每日持仓与资金曲线。
- 模拟实盘使用 Django 调度器在模拟时间的开盘/尾盘窗口触发，根据最近日线数据计算当日因子并生成撮合订单。
- 策略不强制满仓持股，允许空仓；调仓口径固定为每日按综合分选取 Top N。

## 2. 数据基础

当前因子计算主要依赖日线表 `stocks.StockData`：

- `code`：股票代码，如 `sh.600000`、`sz.000001`
- `date`：交易日期
- `close`：收盘价

5 分钟表 `stocks.StockMinuteData` 当前主要用于：

- 行情详情页的分时/分钟 K 线展示
- 日内收益曲线计算
- 持仓市值的盘中估值

注意：当前多因子选股本身没有直接使用 5 分钟数据计算因子，核心因子来自日线收盘价。

## 3. 已实现内置因子

当前内置因子都被设计成“数值越大越好”。前端通过启用开关和权重控制每个因子是否参与打分。

### 3.1 20 日动量因子 `mom_20`

```text
mom_20 = close / close_20 - 1
```

含义：最近 20 个交易日涨幅越高，因子值越大。它用于捕捉中期趋势。

### 3.2 5 日反转因子 `rev_5`

```text
rev_5 = -(close / close_5 - 1)
```

含义：最近 5 个交易日跌幅越大，因子值越高。它用于避免单纯追涨，并给短期回调股票一定加分。

### 3.3 20 日低波动因子 `vol_20`

```text
ret_1 = close / pre_close - 1
vol_20 = -std(ret_1, 20)
```

含义：最近 20 个交易日收益波动越小，因子值越高。它用于降低组合对高波动股票的偏好。

### 3.4 20 日均线趋势因子 `trend_20`

```text
ma_20 = 最近 20 个交易日 close 的平均值
trend_20 = close / ma_20 - 1
```

含义：价格相对 20 日均线越强，因子值越大。它用于确认趋势是否仍然有效。

### 3.5 旧版兼容因子

系统仍保留旧版 JSON 的兼容逻辑：

- `mom_1`：旧版单日动量因子。
- `bias_5`：旧版 5 日均线偏离因子。

旧策略如果只包含 `weight_mom`、`weight_bias`、`top_n`，后端会自动按旧逻辑解析，不会直接报错。但新建策略默认使用 `mom_20`、`rev_5`、`vol_20`、`trend_20`。

## 4. 因子标准化与综合打分

由于不同因子的数值尺度不同，项目使用横截面 Z-Score 标准化。所谓横截面，是指在同一个交易日，把股票池中所有股票放在一起比较。

对每个交易日分别计算：

```text
z_factor = (factor_value - 当日该因子均值) / (当日该因子标准差 + 1e-6)
```

加上 `1e-6` 是为了避免标准差为 0 时除零。

综合分数：

```text
total_score = Σ(z_factor_i * weight_i)
```

最终按 `total_score` 从高到低排序。策略每天取排名靠前的 Top N，若开启空仓且分数低于买入阈值，则对应股票不会进入持仓。

## 5. 回测策略逻辑

回测入口为：

```text
POST /api/backtest/run/
```

前端传入参数：

- `task_name`：回测任务名称
- `start_date`：开始日期
- `end_date`：结束日期
- `initial_capital`：初始资金
- `top_n`：目标持仓数
- `buy_threshold`：买入分数阈值
- `allow_cash`：是否允许空仓
- `fee_rate`：手续费率，当前默认万一，即 `0.0001`
- `min_fee`：最低手续费，当前为 `0`
- `factors`：启用的内置因子及权重

回测流程：

1. 从 `StockData` 读取回测区间内所有股票的日线收盘价。
2. 使用 Spark 按股票代码做时间序列窗口，计算 `mom_20`、`rev_5`、`vol_20`、`trend_20` 等内置因子。
3. 使用 Spark 按交易日做横截面窗口，计算各因子的 Z-Score。
4. 计算综合分数 `total_score`。
5. 将因子矩阵转为 Pandas 状态机，每天按综合分重排股票。
6. 每天只持有 `rank <= top_n` 且 `total_score >= buy_threshold` 的股票。
7. 已持有但不再进入当日 Top N 或不满足买入阈值的股票会被换出。
8. 当没有合格标的且 `allow_cash = true` 时，组合可以空仓。
9. 用当前持仓股票下一日收益率的平均值作为组合毛收益，并扣除换手手续费：

```text
gross_return = mean(next_return of current holdings)
turnover = Σ(abs(new_weight - old_weight))
portfolio_return = gross_return - turnover * fee_rate
```

10. 累乘每日组合收益生成净值曲线：

```text
equity = cumulative_product(1 + portfolio_return)
```

11. 按初始资金换算成资金曲线，返回前端绘图。

当前回测结果包含：

- `total_return`：累计收益率
- `annualized_return`：简单年化收益率
- `final_capital`：期末资金
- `avg_turnover`：平均换手率
- `total_turnover`：累计换手率
- `cash_days`：空仓天数
- `equity_curve`：资金曲线
- `trade_records`：每日入选股票与组合收益

注意：`BacktestResult` 模型中预留了 `max_drawdown`、`sharpe_ratio`、`win_rate` 字段，但当前接口代码尚未计算并写入这些指标。

## 6. 模拟实盘策略逻辑

模拟实盘策略保存在 `trade.Strategy` 表中。需要特别注意：当前 `Strategy.code` 字段并不是 Python 策略代码，而是 JSON 参数配置。例如：

```json
{
  "top_n": 5,
  "buy_threshold": 0.3,
  "allow_cash": true,
  "fee_rate": 0.0001,
  "min_fee": 0,
  "factors": [
    {"code": "mom_20", "weight": 0.45, "enabled": true},
    {"code": "rev_5", "weight": 0.1, "enabled": true},
    {"code": "vol_20", "weight": 0.2, "enabled": true},
    {"code": "trend_20", "weight": 0.25, "enabled": true}
  ]
}
```

股票池保存在 `Strategy.stock_pool`：

- 普通股票池：使用英文逗号分隔，如 `sh.600000,sz.000001`
- 全市场股票池：使用 `__ALL__`、`ALL`、`ALL_STOCKS`、`全部`、`全部股票`

实盘调度逻辑：

1. `clock_tick` 推进系统模拟时间。
2. `TradingEngine.run_all_active_strategies` 在以下窗口触发策略：
   - 开盘窗口：`09:30 - 09:35`
   - 尾盘窗口：`14:50 - 14:59`
3. 引擎只执行 `status = active` 的策略。
4. 同一个策略在同一交易日的同一窗口只执行一次，避免秒级时钟重复下单。

实盘因子计算逻辑：

1. 解析策略 JSON 参数。
2. 展开股票池。
3. 根据启用因子的最大回看窗口，从 `StockData` 批量读取最近一段日线收盘价。
4. 对每只股票计算启用的内置因子。
5. 数据不足的股票不进入本轮因子计算。
6. 使用 Numpy 对股票池做横截面 Z-Score。
7. 按综合分数排序，生成候选股票。

实盘调仓逻辑：

- 卖出：当前持仓不在当日 Top N 候选中时清仓。
- 买入：只买入 `rank <= top_n` 且满足买入阈值的股票。
- 空仓：如果没有合格股票且允许空仓，则不新增持仓。
- 仓位：新买股票按可用现金等额分配。
- 买入数量按 A 股 100 股一手取整：

```text
buy_volume = floor(cash_per_stock / (price * (1 + fee_rate)) / 100) * 100
```

撮合逻辑：

- 当前系统按模拟成交处理，订单创建后直接标记为 `filled`。
- 手续费按万一免五计算：`fee = amount * 0.0001`，最低手续费为 0。
- 买入时扣减 `成交额 + 手续费`，并更新持仓均价。
- 卖出时增加 `成交额 - 手续费`，并减少持仓。
- 成交记录写入 `TradeRecord`。
- 撮合过程使用 `transaction.atomic()` 和 `select_for_update()` 保证资金与持仓更新的一致性。

## 7. 前端配置入口

### 7.1 回测页面

文件：`quant_frontend/src/views/BacktestView.vue`

默认配置：

```text
策略名称：每日多因子轮动策略
初始资金：100000
调仓方式：每日按综合分取 Top N
目标持仓数：5
买入分数阈值：0.3
允许空仓：是
手续费：万一免五
内置因子：mom_20、rev_5、vol_20、trend_20
默认回测区间：2024-01-01 至 2025-12-31
```

前端说明：

- 策略控制区用于配置策略模板、目标持仓数、买入阈值和空仓开关。
- 因子权重区使用表格展示内置因子，避免多个因子平铺成大量滑块。
- 手续费固定展示为万一免五。
- 回测完成后可以一键部署为实盘策略。

### 7.2 实盘策略管理组件

文件：`quant_frontend/src/components/MyStrategies.vue`

可配置项：

- 策略名称
- 股票池，支持手动输入或勾选全部股票
- 允许空仓
- 目标持仓数 Top N
- 买入分数阈值
- 内置因子启用状态与权重
- 运行状态：运行 / 停止

## 8. 现成策略组合

以下组合都可以直接作为 `Strategy.code` 的 JSON 配置使用。它们不是收益承诺，而是不同风险偏好的参数模板，实际效果仍需要通过回测区间验证。

### 8.1 均衡默认组合

```json
{
  "top_n": 5,
  "buy_threshold": 0.3,
  "allow_cash": true,
  "fee_rate": 0.0001,
  "min_fee": 0,
  "factors": [
    {"code": "mom_20", "weight": 0.45, "enabled": true},
    {"code": "rev_5", "weight": 0.1, "enabled": true},
    {"code": "vol_20", "weight": 0.2, "enabled": true},
    {"code": "trend_20", "weight": 0.25, "enabled": true}
  ]
}
```

含义：每天按综合分选出最多 5 只股票，没有合格股票时允许空仓。权重以 20 日动量和 20 日均线趋势为主，保留 20% 低波动约束，并用 10% 短期反转避免过度追涨。

适合：默认推荐、收益与回撤相对均衡的主策略。

### 8.2 高收益进攻组合

```json
{
  "top_n": 3,
  "buy_threshold": 0.1,
  "allow_cash": true,
  "fee_rate": 0.0001,
  "min_fee": 0,
  "factors": [
    {"code": "mom_20", "weight": 0.6, "enabled": true},
    {"code": "rev_5", "weight": 0.05, "enabled": true},
    {"code": "vol_20", "weight": 0.05, "enabled": true},
    {"code": "trend_20", "weight": 0.3, "enabled": true}
  ]
}
```

含义：集中持有 3 只强趋势股票，动量和趋势权重合计 90%，低波动约束很弱，买入阈值较低。

适合：追求更高收益、能接受更大回撤和更高波动的场景。

### 8.3 低回撤防守组合

```json
{
  "top_n": 8,
  "buy_threshold": 0.5,
  "allow_cash": true,
  "fee_rate": 0.0001,
  "min_fee": 0,
  "factors": [
    {"code": "mom_20", "weight": 0.2, "enabled": true},
    {"code": "rev_5", "weight": 0.1, "enabled": true},
    {"code": "vol_20", "weight": 0.5, "enabled": true},
    {"code": "trend_20", "weight": 0.2, "enabled": true}
  ]
}
```

含义：提高持股分散度和买入门槛，低波动因子占 50%，弱市更容易空仓。

适合：希望控制回撤、降低资金曲线波动的场景。

### 8.4 稳健分散组合

```json
{
  "top_n": 6,
  "buy_threshold": 0.25,
  "allow_cash": true,
  "fee_rate": 0.0001,
  "min_fee": 0,
  "factors": [
    {"code": "mom_20", "weight": 0.4, "enabled": true},
    {"code": "rev_5", "weight": 0.05, "enabled": true},
    {"code": "vol_20", "weight": 0.25, "enabled": true},
    {"code": "trend_20", "weight": 0.3, "enabled": true}
  ]
}
```

含义：每天选取最多 6 只股票，仍保留中期趋势和低波动约束，持仓相对更分散。

适合：更重视交易稳定性、希望降低单票集中度的场景。

### 8.5 中期趋势组合

```json
{
  "top_n": 5,
  "buy_threshold": 0.2,
  "allow_cash": true,
  "fee_rate": 0.0001,
  "min_fee": 0,
  "factors": [
    {"code": "mom_20", "weight": 0.6, "enabled": true},
    {"code": "rev_5", "weight": 0.0, "enabled": false},
    {"code": "vol_20", "weight": 0.1, "enabled": true},
    {"code": "trend_20", "weight": 0.3, "enabled": true}
  ]
}
```

含义：更偏好中期强势和均线趋势，适合趋势行情。

### 8.6 短期回调修复组合

```json
{
  "top_n": 5,
  "buy_threshold": 0.1,
  "allow_cash": true,
  "fee_rate": 0.0001,
  "min_fee": 0,
  "factors": [
    {"code": "mom_20", "weight": 0.3, "enabled": true},
    {"code": "rev_5", "weight": 0.4, "enabled": true},
    {"code": "vol_20", "weight": 0.1, "enabled": true},
    {"code": "trend_20", "weight": 0.2, "enabled": true}
  ]
}
```

含义：保留中期趋势过滤，同时更偏好短期回调后的修复机会。

适合：震荡市里寻找短期回调后反弹的场景。

### 8.7 空仓择时严格组合

```json
{
  "top_n": 5,
  "buy_threshold": 0.8,
  "allow_cash": true,
  "fee_rate": 0.0001,
  "min_fee": 0,
  "factors": [
    {"code": "mom_20", "weight": 0.45, "enabled": true},
    {"code": "rev_5", "weight": 0.05, "enabled": true},
    {"code": "vol_20", "weight": 0.25, "enabled": true},
    {"code": "trend_20", "weight": 0.25, "enabled": true}
  ]
}
```

含义：买入分数阈值显著提高，只有因子信号足够强时才进场，否则保持现金。

适合：希望减少弱市持仓暴露、接受可能错过部分行情的场景。

## 9. 策略限制与后续扩展

当前实现已经具备多因子回测和模拟实盘闭环，但仍有一些限制：

1. 因子仍是内置固定集合，暂不支持用户编写自定义表达式因子。
2. 因子计算主要基于日线收盘价，没有使用成交量、换手率、分钟线形态等信息。
3. 回测已经加入换手手续费，但仍未模拟滑点、涨跌停、停牌和真实成交约束。
4. 实盘撮合是模拟立即成交，未处理盘口深度和部分成交。
5. 回测中的 `BacktestResult` 预留了最大回撤、夏普比率、胜率，但当前代码尚未计算。
6. `backtest.models.Factor` 模型已经存在，但当前回测引擎没有动态读取该表，因子仍写死在 `compute_multi_factors` 中。

后续可以扩展的方向：

- 增加成交量因子，例如量比、成交量均线偏离。
- 增加中长期动量因子，例如 20 日、60 日收益率。
- 增加风险控制，例如最大持仓数、单票仓位上限、止损止盈。
- 在回测中加入滑点、涨跌停、停牌和成交量约束。
- 将 `Factor` 模型真正接入回测引擎，实现可配置因子库。
