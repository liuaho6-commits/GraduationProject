先从akshare获取股票日线数据，进行量化分析，将结果存入数据库，由于不同策略可能要使用不同的周期，部分量化因子的计算有半群性质，然后可以使用矩阵优化，随意指定一个周期（如250根k线）都可以快速获取当前的因子值，然后根据因子值和实时用akshare获取的分钟级数据，django调度器分发任务给伪分布式的spark进行计算。

所以需要实现的功能有

1. 管理员界面可能不能只用默认的，需要自己实现一个
2. 需要实现登录功能和用户注册功能。
3. 需要实现策略添加功能，也就是当做一个函数或者类，传给后端，后端根据这个函数进行决策买卖
4. 用户界面需要实现实时收益观看（当天，或者历史收益），开始策略，暂停策略，停止交易。 
5. 用户购买策略，用户自行编写策略，用户回测。
6. 管理员也应该有回测功能。
7. 用户给股票账户转账（当然是模拟）
8. 为了简化交易，直接使用买一卖一，无论单多大，一定能成交。



请你告诉我应该要怎么做，时间还有两个月，比较充裕，没有课，我是不是应该先学习后端的设计，和各种功能，对于数据库，假如要用了再增加表，后端开发完成后，再开发前端，同时检查后端的正确性



12/27 17:38

### 1. 基础数据管理层 (Data Layer)

- **股票信息存储**：实现了 `StockBasicInfo` 模型，存储了 3000+ 只股票的代码和名称。
- **历史行情存储**：实现了 `StockData` 模型，支持海量日线数据的存储与快速查询（含联合索引优化）。
- **大数据导入脚本**：编写了高效的 Python 脚本，支持通过 `bulk_create` 批量将根目录下的 CSV 历史行情文件导入 MySQL 数据库。

### 2. 量化交易引擎 (Trading Engine)

- **模拟撮合系统**：实现了 `MockExchange` 类，模拟真实交易所逻辑。支持买一卖一即时成交，并自动计算**移动加权平均持仓成本**。
- **资产快照逻辑**：开发了资产计算工具，可实时根据当前持仓和最新股价计算“可用资金 + 持仓市值”的总资产。

### 3. 策略开发与执行沙箱 (Strategy Engine)

- **Python 策略沙箱**：基于 `exec` 实现了策略执行器 `StrategyExecutor`。
  - **注入式 API**：策略代码中可直接调用 `buy()`、`sell()`、`get_price()`、`get_pool()` 和 `log()` 等内置函数。
  - **股票池支持**：支持用户为不同策略指定特定的股票池，提高选股效率。
- **分布式调度系统**：
  - **策略自动运行**：基于 `APScheduler` 实现了定时任务，每分钟自动轮询并执行所有“运行中”的策略。
  - **自动化记账**：每 5 分钟自动记录一次用户资产快照，用于生成收益曲线。

### 4. 前端交互界面 (Frontend UI)

- **通用布局架构**：实现了 `MainLayout` 布局，确保侧边栏导航、顶部用户信息在页面切换时保持固定。
- **策略控制台**：
  - **实时监视器**：直观展示总资产、策略状态（运行/暂停/停止）。
  - **在线编辑器**：支持用户在线编写 Python 策略代码并设置选股池。
  - **交易记录列表**：实时展示该策略产生的历史委托和成交单据。
- **行情中心**：实现了带分页功能的行情列表，支持查看全市场股票价格并一键复制代码。

### 5. 用户与权限体系 (User & Auth)

- **账户扩展系统**：通过 `UserProfile` 扩展了 Django 原生 User 模型，存储模拟资金、手机号等关键信息。
- **Token 认证机制**：前后端采用 REST Framework 的 Token 机制进行安全通信。
- **管理员后台**：
  - **专属登录**：限制仅超级管理员可登录管理端。
  - **系统监控**：提供仪表盘功能，统计总用户数、总股票数及系统总资金池。

**目前的系统状态：** 你已经搭建好了一个可以“实战”的框架。用户可以登录、入金、在行情中选股、编写策略并让它在后台自动买卖，同时还能在控制台看到实时的资产变动。





11.30







 



